<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miaoyu.naigos.user.mapper.TransArchiveMapper">
    <select id="getOldArchiveByQqId" resultType="com.miaoyu.naigos.user.entity.OldArchiveEntity">
        SELECT * FROM archives WHERE qq_id = #{qqId}
    </select>
</mapper>